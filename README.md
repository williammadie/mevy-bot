# Mevy Bot

## Commands

Run RAG tests
```bash
deepeval test run tests/retriever/test_retriever.py
```

Run Qdrant DB
```bash
cd devstack
docker compose up -d
```

- Dashboard URL: `http://localhost:6333/dashboard`